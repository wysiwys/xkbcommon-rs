#[allow(dead_code)]
fn fuzz_keymap_format() {
    // TODO: see fuzz/fuzz.sh

    /*
    // TODO: command line inputs
    let ctx = Context::new(ContextFlags::NO_DEFAULT_INCLUDES | ContextFlags::NO_ENVIRONMENT_NAMES)
        .unwrap();

    // TODO: AFL?
    let file = todo!();
    let _keymap =
        Keymap::new_from_file(ctx, file, KeymapFormat::TextV1, CompileFlags::empty()).unwrap();

    */
}
